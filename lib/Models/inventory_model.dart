class InventoryModel {
  var id;
  var name;
  var weidth;
  var Image;
  var stockid;

  InventoryModel(
      {required this.id,
        required this.name,
        required this.weidth,
        required this.Image,required this.stockid});
}
